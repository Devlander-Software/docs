import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Development Documentation System',
  description: 'Comprehensive development standards and guidelines for modern software development teams',
  keywords: 'development, documentation, standards, guidelines, typescript, react, testing, security',
  authors: [{ name: 'Development Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
